Pause or activate one or more campaigns.

Use the `mcp__meta-ads__update_campaign_status` tool.

Required from user:
- campaign_ids: One or more campaign IDs
- status: ACTIVE or PAUSED

Confirm the status change for each campaign.

$ARGUMENTS
