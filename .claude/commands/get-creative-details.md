Get full creative details for an ad including copy text (primary_text, headline, description), CTA, and image/video URLs.

Use the `mcp__meta-ads__get_creative_details` tool.

Required from user:
- ad_id: The ad ID to inspect

Present the creative details clearly: primary text, headline, description, CTA, and media URLs.

$ARGUMENTS
