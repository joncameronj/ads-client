Pause or activate one or more ad sets.

Use the `mcp__meta-ads__update_adset_status` tool.

Required from user:
- adset_ids: One or more ad set IDs
- status: ACTIVE or PAUSED

Confirm the status change for each ad set.

$ARGUMENTS
