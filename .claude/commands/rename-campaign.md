Rename a campaign in Meta Ads Manager.

Use the `mcp__meta-ads__rename_campaign` tool.

Required from user:
- campaign_id: The campaign ID to rename
- name: The new campaign name

Confirm the rename showing the previous and new name.

$ARGUMENTS
