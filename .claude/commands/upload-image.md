Upload a local image file to Meta as an ad image. Returns the image_hash for use in /create-creative. Supports JPG, PNG. Max 30MB.

Use the `mcp__meta-ads__upload_image` tool.

Required from user:
- file_path: Absolute path to local image file

Return the image_hash so the user can use it with /create-creative.

$ARGUMENTS
